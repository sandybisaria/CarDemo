#include "ToBullet.hpp"

#ifdef COMPILE_UNIT_TESTS
#include <gtest/gtest.h>

#define EXPECT_NEAR_HP(a, b) EXPECT_NEAR(a, b, 0.0001)

TEST(ToBullet, ToBulletFunctions) {
	EXPECT_TRUE(true); // Skip for now; veeeery unlikely to be broken
}

#endif
